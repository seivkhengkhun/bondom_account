# Secrets — never commit real tokens
.env

# Databases — production data must never live in git
*.db
backups/

# Python
.venv/
__pycache__/
*.pyc

# Reflex build artifacts
app/web/.web/
app/web/.states/
assets/external/
